/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Opcional, melhora depuração
    // Sem 'output: "export"' para garantir modo dinâmico
  };

export default nextConfig;
